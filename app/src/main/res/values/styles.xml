<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="linksTransparente" parent="@android:style/Widget.TextView">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="inputLoginCadastro">
        <item name="android:layout_width">299dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:textColorHint">@color/fontAndImagesColorPadrao</item>
        <item name="boxBackgroundMode">outline</item>
        <item name="hintAnimationEnabled">true</item>
        <item name="hintEnabled">true</item>
        <item name="passwordToggleTint">@color/fontAndImagesColorPadrao</item>
    </style>

</resources>

